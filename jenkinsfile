pipeline {
    agent any 
    stages {
        stage('List env vars') {
            steps {
                script {
                    checkout scm
                }
            }
        }

    }
}
